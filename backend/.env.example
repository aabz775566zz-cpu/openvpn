# Server
PORT=3000
NODE_ENV=development

# Service metadata
SERVICE_NAME=OpenWorld VPN Backend

# API
API_PREFIX=/api/v1

# Logging
LOG_LEVEL=info

# Database (not connected yet — placeholder for a future phase)
# DATABASE_URL=postgresql://<user>:<password>@<host>:5432/openworld_vpn
