# Placeholder Docker Compose configuration for local development.
# Real service definitions will be added in a future development step.
version: "3.9"

services:
  api:
    build:
      context: ../../backend
    env_file:
      - ../../backend/.env.example
    ports:
      - "3000:3000"
