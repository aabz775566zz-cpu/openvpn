# Placeholder CI workflow for OpenWorld VPN.
# Real lint/build/test steps will be added as each component is implemented.
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder step
        run: echo "CI pipeline to be implemented alongside each component."
